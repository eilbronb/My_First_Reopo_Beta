---
  - name: Fact Finder
    hosts: nexus
    tasks:
      - name: Register Ansible Facts
        setup:
        register: ansible_facts
      - name: Print Facts
        debug:
          msg: "{{ ansible_facts }}"
